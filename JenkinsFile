pipeline {
    agent {
        docker {
        image 'node:16.3.0-alpine'
        }
    }
  environment {
    CI = 'true'
  }
    stages {
        stage('Build') {
      steps {
        sh 'node -v'
        sh 'npm install --verbose'
      }
        }
    }
}
